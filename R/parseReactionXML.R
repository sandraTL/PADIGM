#' Classe to get substrate and product of reaction list obtain from map KGML
#'

### ortholog
# getKgmlPairOrthologReactions <- function(kgmlEdgeList){
#
#   # return all rows with type = ortholog
#  orthologDataFrame <- kgmlEdgeList[grep("ortholog",
#                                         kgmlEdgeList$type, ignore.case=T)];
#
#
#
#  return <- orthologDataFrame;
#
#
# }

# pas fonctionnelle, ortholog
# getMainReactions <- function(reactions){
#
#     # return all rows with type = ortholog
#     reactionDT <- reactions[grep("main", reactions, ignore.case=T)];
#       print(c("dddddd : ",reactionDT));
#
#
#     return <- reactionDT;
#
#
# }
